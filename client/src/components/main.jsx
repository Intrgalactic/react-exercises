import { Search } from "./search"
import { Countries } from "./countries"
export const Main = ({handleWrite,countries,handleRedirect,children}) => {
    return (
        <main>
            {children}
        </main>
    )
}